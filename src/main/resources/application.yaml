spring:
  application:
    name: studendgradetracker
  datasource:
    url: jdbc:postgresql://localhost:5432/app
    username: postgres
    password: amoh2068
    driver-class-name: org.postgresql.Driver
  sql:
    init:
      mode: always
      schema-locations: classpath:schema.sql